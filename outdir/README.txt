cented_discovery_master.tsv	Per-mint Cented trade summary from wallet_token_flow rows for --cented-wallet.
cented_trigger_deltas.tsv	Per-mint timing markers and deltas to first_buy_ts from earliest detectable events.
cented_deployer_features.tsv	Per-mint deployer/program/authority/deployer-history features (best effort from available tables).
cented_routing_footprint.tsv	Program invocation footprint for Cented first-buy tx by mint.
cented_fast_win_loss_feature_ranking.tsv	Top 15 features separating FAST_WIN vs FAST_LOSS with coverage.

Missing derivations / required tables:
- Database file not found: masterwalletsdb.db. Needed: masterwalletsdb.db or explicit --db path.
